# Optional runtime settings. This file is loaded automatically via python-dotenv.

# Timeout in seconds for each outbound search request.
REQUEST_TIMEOUT_SECONDS=20

# Pause in seconds between consecutive search queries (helps avoid rate limiting).
SEARCH_DELAY_SECONDS=1.5
